title: KEP Template
kep-number: 3908
authors:
  - "@ivelichkovich"
  - "@micahhausler"
owning-sig: sig-auth
participating-sigs:
  - sig-auth
status: provisional|implementable|implemented|deferred|rejected|withdrawn|replaced
creation-date: 2023-02-01
reviewers:
  - "@enj"
  - "@mikedanese"
approvers:
  - TBD

see-also:
  - "/keps/sig-auth/740-service-account-external-signing"
  - https://github.com/kubernetes/kubernetes/pull/73110
  - https://github.com/kubernetes/enhancements/pull/704
replaces:
  - "/keps/sig-auth/740-service-account-external-signing"

# The target maturity stage in the current dev cycle for this KEP.
stage: alpha

# The most recent milestone for which work toward delivery of this KEP has been
# done. This can be the current (upcoming) milestone, if it is being actively
# worked on.
latest-milestone: "v1.28"

# The milestone at which this feature was, or is targeted to be, at each stage.
milestone:
  alpha: "v1.28"
  beta: "v1.29"
  stable: "v1.31"

# The following PRR answers are required at alpha release
# List the feature gate name and the components for which it must be enabled
feature-gates:
  - name: ExternalKeyService
    components:
      - kube-apiserver
disable-supported: true

# The following PRR answers are required at beta release
metrics:
  - apiserver_external_key_server_duration_seconds
  - apiserver_external_key_server_failure_count
