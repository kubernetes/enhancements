title: Gang Policy for Job
kep-number: 5764
authors:
  - "@kannon92"
owning-sig: sig-apps
participating-sigs:
  - sig-scheduling
status: implementable
creation-date: 2025-12-29
reviewers:
  - "tenzen-y"
approvers:
  - "@soltysh"

see-also:
  - "/keps/sig-apps/2232-suspend-jobs"
  - "/keps/sig-scheduling/2926-job-mutable-scheduling-directives"

# The target maturity stage in the current dev cycle for this KEP.
stage: alpha

# The most recent milestone for which work toward delivery of this KEP has been
# done. This can be the current (upcoming) milestone, if it is being actively
# worked on.
latest-milestone: "v1.36"

# The milestone at which this feature was, or is targeted to be, at each stage.
milestone:
  alpha: "1.36"

# The following PRR answers are required at alpha release
# List the feature gate name and the components for which it must be enabled
feature-gates:
  - name: JobGangPolicy
    components:
      - kube-apiserver
      - kube-controller-manager

# Feature gate dependencies - JobGangPolicy requires these feature gates to be enabled
# - GangScheduling: Enables gang scheduling support in the scheduler
# - GenericWorkload: Enables the Workload API in scheduling.k8s.io
disable-supported: true

# The following PRR answers are required at beta release
metrics:
  - job_sync_duration_seconds
  - job_syncs_total
