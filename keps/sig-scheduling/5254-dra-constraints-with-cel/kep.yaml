title: DRA Constraints with CEL
kep-number: 5254
authors:
  - "@aws-ruhip"
  - "@geetasg"
owning-sig: sig-scheduling
status: implementable
creation-date: 2025-06-10
reviewers:
  - "@deads2k"
  - "@macsko"
  - "@pohly"
approvers:
  - "@alculquicondor" # SIG Scheduling

see-also:
  - "/keps/sig-node/4381-dra-structured-parameters"

# The target maturity stage in the current dev cycle for this KEP.
stage: alpha

# The most recent milestone for which work toward delivery of this KEP has been
# done. This can be the current (upcoming) milestone, if it is being actively
# worked on.
latest-milestone: "v1.34"

# The milestone at which this feature was, or is targeted to be, at each stage.
milestone:
  alpha: "v1.34"

# The following PRR answers are required at alpha release
# List the feature gate name and the components for which it must be enabled
feature-gates:
  - name: DRACELDeviceConstraint
    components:
      - kube-apiserver
      - kube-controller-manager
      - kube-scheduler
disable-supported: true

# The following PRR answers are required at beta release
metrics:
  - resourceclaim_controller_with_cel_constraint
  - resourceclaim_controller_with_match_attribute_constraint
  - resourceclaim_controller_with_cel_constraint_latency
