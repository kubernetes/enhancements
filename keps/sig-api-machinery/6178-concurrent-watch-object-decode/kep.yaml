title: Concurrent Watch Object Decode
kep-number: 6178
authors:
  - "@jefftree"
  - "@serathius"
owning-sig: sig-api-machinery
status: implementable
creation-date: 2026-06-08
reviewers:
  - "@jpbetz"
approvers:
  - "@jpbetz"

see-also: []
replaces: []

# The target maturity stage in the current dev cycle for this KEP.
stage: beta

# The most recent milestone for which work toward delivery of this KEP has been
# done. This can be the current (upcoming) milestone, if it is being actively
# worked on.
latest-milestone: "v1.37"

# The milestone at which this feature was, or is targeted to be, at each stage.
# The ConcurrentWatchObjectDecode gate was introduced as Beta (disabled by
# default) in v1.31. It is enabled by default in v1.37.
milestone:
  beta: "v1.31"

# The following PRR answers are required at alpha release
# List the feature gate name and the components for which it must be enabled
feature-gates:
  - name: ConcurrentWatchObjectDecode
    components:
      - kube-apiserver
disable-supported: true

# The following PRR answers are required at beta release
metrics:
  - apiserver_watch_cache_initializations_total
  - apiserver_watch_cache_initialization_errors_total
  - apiserver_request_duration_seconds
