title: Kustomize Plugin Graduation
kep-number: 2953
authors:
  - "@knverey"
owning-sig: sig-cli
participating-sigs:
  - sig-cli
status: provisional
creation-date: 2021-09-07
reviewers:
  - "@monopole"
  - "@pwittrock"
  - "@natasha41575"
approvers:
  - "@monopole"

see-also:
  - "/keps/sig-cli/2299-kustomize-plugin-composition"
  - "https://github.com/kubernetes/enhancements/pull/2908"
  - "/keps/sig-cli/2385-kustomize-secret-generator-plugins/README.md"
  - "/keps/sig-cli/2382-kustomize-exec-secret-generator/README.md"
replaces:
  - "/keps/sig-cli/993-kustomize-generators-transformers/README.md"

# The target maturity stage in the current dev cycle for this KEP.
stage: alpha

# The most recent milestone for which work toward delivery of this KEP has been
# done. This can be the current (upcoming) milestone, if it is being actively
# worked on.
latest-milestone: "v1.24"

# The milestone at which this feature was, or is targeted to be, at each stage.
milestone:
  beta: TBD
  stable: TBD

# # The following PRR answers are required at alpha release
# # List the feature gate name and the components for which it must be enabled
# feature-gates:
#   - name: n/a
#     components:
#       - n/a
# disable-supported: true

# # The following PRR answers are required at beta release
# metrics:
#   - n/a
