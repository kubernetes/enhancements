title: kubectl reverse port-forward
kep-number: 5695
authors:
  - "@willianpaixao"
owning-sig: sig-cli
participating-sigs:
  - sig-cli
  - sig-node
  - sig-api-machinery
status: provisional
creation-date: 2025-11-18
reviewers:
  - TBD
approvers:
  - TBD
see-also:
  - "https://github.com/kubernetes/kubernetes/issues/20227"
  - "https://github.com/kubernetes/kubernetes/pull/57320"

# The target maturity stage in the current dev cycle for this KEP.
stage: alpha

# The most recent milestone for which work toward delivery of this KEP has been
# done. This can be the current (upcoming) milestone, if it is being actively
# worked on.
latest-milestone: "v1.34"

# The milestone at which this feature was, or is targeted to be, at each stage.
milestone:
  alpha: "v1.34"
  beta: "v1.35"
  stable: "v1.36"

# The following PRR answers are required at alpha release
# List the feature gate name and the components for which it must be enabled
feature-gates:
  - name: KubectlReversePortForward
    components:
      - kubectl
      - kubelet
disable-supported: true

# The following PRR answers are required at beta release
metrics:
  - kubectl_portforward_reverse_active_connections
  - kubectl_portforward_reverse_connection_duration_seconds
  - kubectl_portforward_reverse_connections_total
  - kubectl_portforward_reverse_errors_total
  - kubelet_portforward_reverse_listeners_active
