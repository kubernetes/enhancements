title: Exec session identity propagation
kep-number: 6035
authors:
  - "@sigtrapd"
  - "@ngopalak-redhat"
  - "@nispriha"

owning-sig: sig-node
participating-sigs:
  - sig-security
  - sig-api-machinery

status: implementable
creation-date: 2026-04-28
reviewers:
  - "@haircommander"

approvers:
  - "@haircommander"

see-also:
  - "/keps/sig-node/6090-exec-env-injection"
  - "https://blog.sigtrapd.dev/posts/k8s-exec-identity-gap/"
  - "https://blog.sigtrapd.dev/posts/exploring-the-exec-identity-gap/"
  - "https://blog.sigtrapd.dev/posts/bridging-k8s-exec-identity-gap/"

replaces:
  - "6090"

# The target maturity stage in the current dev cycle for this KEP.
# If the purpose of this KEP is to deprecate a user-visible feature
# and a Deprecated feature gates are added, they should be deprecated|disabled|removed.
stage: alpha

# The most recent milestone for which work toward delivery of this KEP has been
# done. This can be the current (upcoming) milestone, if it is being actively
# worked on.
latest-milestone: "v1.37"

# The milestone at which this feature was, or is targeted to be, at each stage.
milestone:
  alpha: "v1.37"
  beta: "v1.38"
  stable: "v1.39"

# The following PRR answers are required at alpha release
# List the feature gate name and the components for which it must be enabled
feature-gates:
  - name: ExecRequestID
    components:
      - kube-apiserver
      - kubelet
  - name: ExecEnvVar # This FG is required for the above FG to work. 
    components:
      - kube-apiserver
      - kubelet

disable-supported: true

# The following PRR answers are required at beta release
metrics:
  - apiserver_exec_audit_id_injected_total
