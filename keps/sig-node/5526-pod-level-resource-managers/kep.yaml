title: Pod-Level Resource Managers
kep-number: 5526
authors:
  - "@KevinTMtz"
owning-sig: sig-node
participating-sigs:
  - sig-node
# provisional|implementable|implemented|deferred|rejected|withdrawn|replaced
status: implementable
creation-date: 2025-09-10
reviewers:
  - "@ndixita" # sig-node-assigned-reviewer
  - "@ffromani"
  - "@tallclair" # sig-node-assigned-reviewer
approvers:
  - "@dchen1107" # sig-node-tl

see-also:
  - "/keps/sig-node/2837-pod-level-resource-spec"
replaces: []

# The target maturity stage in the current dev cycle for this KEP.
# If the purpose of this KEP is to deprecate a user-visible feature
# and a Deprecated feature gates are added, they should be deprecated|disabled|removed.
# alpha|beta|stable
stage: alpha

# The most recent milestone for which work toward delivery of this KEP has been
# done. This can be the current (upcoming) milestone, if it is being actively
# worked on.
latest-milestone: "v1.36"

# The milestone at which this feature was, or is targeted to be, at each stage.
milestone:
  alpha: "v1.36"
  beta: "v1.37"
  stable: "v1.39"

# The following PRR answers are required at alpha release
# List the feature gate name and the components for which it must be enabled
feature-gates:
  # Already existing feature for `KEP-2837: Pod Level Resource Specifications`
  - name: PodLevelResources
    components:
      - kubelet
  # New feature gate, dependent on `PodLevelResources`
  - name: PodLevelResourceManagers
    components:
      - kubelet

disable-supported: true

# The following PRR answers are required at beta release
metrics:
  - resource_manager_allocations_total
  - resource_manager_allocation_errors_total
  - resource_manager_container_assignments
